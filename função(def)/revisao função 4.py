'''
Crie uma função que calcula o valor do frete da seguinte forma:
 Valor = (peso x 0.5) + (distancia x 0.1) + taxa_fixa
 '''

def calcular_peso(peso):
    peso1 * 0.5
    
def calcular_distancia(distancia):
    distancia1 * 0.1
    
def valor_final(peso, distancia, taxa):
    calcular_peso() + calcular_distancia() + taxa_fixa1
    
    
peso1 = int(input( " Digite o peso do produto: "))
distancia1 = int(input( " Digite a distancia: "))
taxa_fixa1 = 15
valor = (peso1 * 0.5) + (distancia1 * 0.1) + taxa_fixa1

valor_final()